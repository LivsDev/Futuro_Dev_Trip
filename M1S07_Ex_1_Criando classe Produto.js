class Produto {
    constructor(nome, preco, quantidade) {
        this.nome = nome;        // Atributo para o nome do produto
        this.preco = preco;      // Atributo para o preço do produto
        this.quantidade = quantidade;  // Atributo para a quantidade em estoque
    }
}